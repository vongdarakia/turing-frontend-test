import React from 'react';

import RegisterComponent from './RegisterComponent';
import storiesOf from '../../../stories/stories-of';

storiesOf('RegisterComponent', module).add('default', () => (
    <RegisterComponent />
));
